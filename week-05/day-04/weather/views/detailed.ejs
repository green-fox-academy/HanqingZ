<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link href="https://fonts.googleapis.com/css?family=Lato:400,900" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/static/detailed.css">
    <title>Weather Dashboard</title>
  </head>
  <body>
    <header>
      <h1 onclick="location='/'"><%= forecasts[id].city %></h1>
      <%- include("navigation.ejs", {forecasts}) %>
    </header>
    <main>
      <% forecasts[id].weather.forEach(element => { %>
        <div>
          <img src="/static/<%=element.icon %>.png" />
          <p><%=element.temp %>°C</p>
          <p><%=element.message %></p>
        </div>
      <% }) %>
    </main>
  </body>
</html>